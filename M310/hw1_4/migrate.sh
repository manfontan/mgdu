# vagrant@database:~/shared$ sudo mongo admin -u alice -p secret
# MongoDB shell version: 3.2.12
# connecting to: admin
# MongoDB Enterprise > db.adminCommand({authSchemaUpgrade: 1});
# { "done" : true, "ok" : 1 }
